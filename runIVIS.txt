#!/bin/bash
# Code to unzip csv, calculate IVIS, and rezip files

ml R
#wget http://cran.us.r-project.org
#R CMD INSTALL nparACT_0.8.tar.gz

DATAIVIS=/scratch/groups/jzeitzer/UKBB/Data/IVIS/Batch_fullfiles
export DATAIVIS
DATACSV=/scratch/groups/jzeitzer/UKBB/Data/Outputs/Batch_fullfiles/timeSeries
export DATACSV

while IFS= read -r LINE
do

    echo "$FILE :: $LINE"

    BASELINE="$DATACSV/${LINE}"
    export BASELINE
    echo $BASELINE
    R < /scratch/groups/jzeitzer/UKBB/Code/ukbb/calcIVIS.R --save

#    for csvfn in $DATACSV/$BASELINE*; do
#        export csvfn
#        R < /scratch/groups/jzeitzer/UKBB/Code/ukbb/calcIVIS.R --save
#    done 

done < "$FILE"
